# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Maintainer: Your Name <youremail@domain.com>
pkgname=st
pkgver=0.8.2
pkgrel=2
pkgdesc="The Simple (Suckless) Terminal"
arch=('x86_64' 'i386')
url="https://st.suckless.org/"
license=('MIT')
depends=('libx11')
provides=('st')
source=("https://dl.suckless.org/st/$pkgname-$pkgver.tar.gz"
        "https://st.suckless.org/patches/alpha/$pkgname-alpha-$pkgver.diff"
        "$pkgname-$pkgver-myconfig.diff")
sha256sums=('aeb74e10aa11ed364e1bcc635a81a523119093e63befd2f231f8b0705b15bf35'
            'SKIP' 'SKIP')

prepare() {
	cd "$pkgname-$pkgver"
	patch <"$srcdir/$pkgname-alpha-$pkgver.diff"
	patch <"$srcdir/$pkgname-$pkgver-myconfig.diff"
}

build() {
	cd "$pkgname-$pkgver"
	make
}

package() {
	cd "$pkgname-$pkgver"
	make PREFIX=/usr MANPREFIX=/usr/share/man DESTDIR="$pkgdir/" install
}
